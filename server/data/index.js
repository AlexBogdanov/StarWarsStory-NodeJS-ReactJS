const userData = require('./user-data');
const characterData = require('./character-data');
const spaceshipData = require('./spaceship-data');

module.exports = {
    userData,
    characterData,
    spaceshipData
}