import React from 'react';
import './ListItem.css';

const ListItem = ({ name, shortDescr, imageUrl }) => {
    return (
        <div className="ListItem">

        </div>
    );
};

export default ListItem;
